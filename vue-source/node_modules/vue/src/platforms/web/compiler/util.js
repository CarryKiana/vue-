/* @flow */

import { makeMap } from 'shared/util'
// 判断是否是一元标签
export const isUnaryTag = makeMap(
  'area,base,br,col,embed,frame,hr,img,input,isindex,keygen,' +
  'link,meta,param,source,track,wbr'
)

// Elements that you can, intentionally, leave open
// (and which close themselves)
// 这些元素你可以故意的保持开放状态，即不写结束标签，它们会自闭合（即浏览器会检测补足）
// 判断是否能当作是自闭合标签
export const canBeLeftOpenTag = makeMap(
  'colgroup,dd,dt,li,options,p,td,tfoot,th,thead,tr,source'
)

// HTML5 tags https://html.spec.whatwg.org/multipage/indices.html#elements-3
// Phrasing Content https://html.spec.whatwg.org/multipage/dom.html#phrasing-content
// 用来判断是否是非语句型标签，语句型标签用来标记段落级文本
export const isNonPhrasingTag = makeMap(
  'address,article,aside,base,blockquote,body,caption,col,colgroup,dd,' +
  'details,dialog,div,dl,dt,fieldset,figcaption,figure,footer,form,' +
  'h1,h2,h3,h4,h5,h6,head,header,hgroup,hr,html,legend,li,menuitem,meta,' +
  'optgroup,option,param,rp,rt,source,style,summary,tbody,td,tfoot,th,thead,' +
  'title,tr,track'
)
// 按照html5元素内容模型来区分，分为
// 元数据型（metadata content）: 用于说明其他内容的表现或行为，或者在当前文档和其他文档之间建立联系的元素（base、link、meta、noscript、script、等）
// 区块型（sectioning content）: 用于定义标题及页脚范围的元素（article、aside、nav、section）
// 标题型（heading content）: 用于定义一个区块/章节的标题（h1、h2、h3、h4、h5、h6）
// 文档流型（flow content）: 用于在应用程序或文档的主体中使用的大部分元素（a、abbr、article、button、div等）
// 语句型（phrasing content）: 用于标记段落级文本的元素（a、abbr、area等）
// 内嵌型(或叫嵌入型)（embedded content）:用于引用或插入到文档中其他资源的元素（audio、canvas、embed、iframe、img、math、object、svg、video）
// 交互型（interactive content）: 专门用于与用户交互的元素（a、audio（如果设置了controls属性）、button等）
// 元素不属于任何一个类别，称为穿透的；元素可能属于不止一个类别，称为混合的